const BlogLayout =({children})=>{
  return(
    <main className="max-w[80%] bg-amber-100 mx-auto grid place-items-center text-slate-950">
      {children}
    </main>
  )
}

export default BlogLayout 